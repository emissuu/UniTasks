<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Class="UI2.Views.MainView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="TextBlockEventTitle" Margin="20" Foreground="White" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#333" CornerRadius="10" Margin="10" Padding="10">
                <!-- <StackPanel VerticalAlignment="Stretch">
					<TextBlock Text="Event blocks" FontSize="18" Foreground="White" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                    <ListBox Name="EventBlocksListBox" ItemsSource="{Binding EventBlocks}" SelectedItem="{Binding SelectedEventBlock}" SelectionChanged="EventBlocksListBox_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Name}" FontSize="16" Foreground="White"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,-12">
                                        <TextBlock Text="{Binding StartTime, StringFormat='{}{0:HH:mm}'}" Margin="0,0,36,0" FontSize="14" Foreground="LightGray"/>
                                        <TextBlock Text="to" FontSize="14" Foreground="LightGray" Margin="0,0,36,0"/>
                                        <TextBlock Text="{Binding EndTime, StringFormat='{}{0:HH:mm}'}" FontSize="14" Foreground="LightGray"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel> -->
            </Border>
            <Border Grid.Column="1" Background="#333" CornerRadius="10" Margin="10" Padding="10">
                
            </Border>
        </Grid>
    </Grid>
</UserControl>
