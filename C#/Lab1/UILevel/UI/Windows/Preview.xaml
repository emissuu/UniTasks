<Window x:Class="DataProcessing.UI.Windows.Preview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataProcessing.UI.Windows"
        mc:Ignorable="d"
        Title="Preview" Height="420" Width="800" MinHeight="420" MinWidth="600" MaxHeight="420">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                BorderBrush="DarkGray" BorderThickness="0,0,2,0">
            <DataGrid Name="PreviewDataGrid" Grid.Row="1"
                      Margin="4" BorderBrush="Gray" BorderThickness="1"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdPlusOne}"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                    <DataGridTextColumn Header="Artist" Binding="{Binding Artist}"/>
                    <DataGridTextColumn Header="Release date" Binding="{Binding ReleaseDate}"/>
                    <DataGridTextColumn Header="Genres" Binding="{Binding Genres}"/>
                    <DataGridTextColumn Header="User score" Binding="{Binding User_Score}"/>
                    <DataGridTextColumn Header="Rating count" Binding="{Binding Rating}"/>
                    <DataGridTextColumn Header="Album link" Binding="{Binding Album_Link}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Name="Maintext" Grid.Column="0" VerticalAlignment="Center"
                       Margin="4,0,4,4" FontSize="12" Content="data"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Width="60" Margin="4" Content="Cancel"
                        Click="Cancel_Click"/>
                <Button Width="60" Margin="4" Content="Load"
                        Click="Load_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
